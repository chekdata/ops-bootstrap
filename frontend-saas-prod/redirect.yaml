apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-saas-http-redirect
  namespace: frontend-saas-prod
  labels:
    app: frontend-saas-http-redirect
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-saas-http-redirect
  template:
    metadata:
      labels:
        app: frontend-saas-http-redirect
    spec:
      containers:
        - name: redirect
          image: chek-images-cn-beijing.cr.volces.com/devops/yapi:1.10.2
          command: ["node"]
          args:
            - "-e"
            - |
              require("http")
                .createServer((req,res)=>{
                  res.statusCode=301;
                  res.setHeader("Location","https://"+req.headers.host+req.url);
                  res.end();
                })
                .listen(8080);
          ports:
            - name: http
              containerPort: 8080
          resources: {}
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-saas-http-redirect
  namespace: frontend-saas-prod
  labels:
    app: frontend-saas-http-redirect
spec:
  type: NodePort
  selector:
    app: frontend-saas-http-redirect
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP


