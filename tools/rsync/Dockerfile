FROM alpine:3.20

# Install rsync and OpenSSH client for secure copy
RUN apk add --no-cache rsync openssh-client

# Default command keeps the container running for ad-hoc exec
CMD ["sleep", "infinity"]


