apiVersion: v1
kind: Service
metadata:
  name: http-redirect
  namespace: platform-staging
  labels:
    app.kubernetes.io/name: http-redirect
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP


