apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-app
  namespace: app-dev
  labels:
    app.kubernetes.io/name: frontend-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frontend-app
    spec:
      containers:
        - name: frontend-app
          image: chek-images-cn-beijing.cr.volces.com/dev/frontend-app:sha-4dfe31cc7b6a5c2af7cdd7e55b26ed53b0db992a
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              protocol: TCP
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5


